# Implementation Plans

Generated by the `improve` skill on 2026-08-02 against commit `325f1ee`.
Execute in the order below unless dependencies say otherwise. Each executor
must read its plan fully, honor its STOP conditions, and update the status row
when done.

## Execution order & status

| Plan | Title | Priority | Effort | Depends on | Status |
|------|-------|----------|--------|------------|--------|
| 001 | Establish one reliable verification command | P1 | M | — | DONE |
| 002 | Remove the private iOS blur API | P1 | M | 001 | IN PROGRESS — code/scan/build done; visual matrix (4 headers × scroll, touch, rotation, light/dark, physical device) not yet verified |
| 003 | Make image imports idempotent and retry-safe | P1 | L | 001 | DONE |
| 004 | Make incoming shares recoverable | P1 | L | 003 | IN PROGRESS — automated tests pass (104); Step 5 manual iOS/Android device matrix (retry, remount, process-termination) still pending |
| 005 | Make Tidy saves durable | P1 | L | 003 | TODO |
| 006 | Preserve Tidy deletions when confirmation fails | P1 | M | 005 | TODO |
| 007 | Enforce a safe backend URL-fetch policy | P1 | L | 001 | IN PROGRESS — code/tests/check/SSRF-scan done (167 tests); live dev-deployment manual verification (Step 6) still pending |
| 008 | Keep optional AI enrichment from failing ready items | P1 | M | 007 | TODO |

Status values: TODO | IN PROGRESS | DONE | BLOCKED (with one-line reason) |
REJECTED (with one-line rationale).

## Dependency notes

- 001 is the prerequisite for every behavior change because the current repo
  has no test command or CI gate and lint is red.
- 003 defines the per-image result and idempotency contract consumed by the
  incoming-share and Tidy save flows.
- 005 and 006 both edit Tidy orchestration. Run them serially; 006 assumes the
  save lifecycle from 005 is already present and must preserve it.
- 007 and 008 both edit `convex/ai.ts`. Run 007 first to avoid parallel merge
  conflicts and ensure the AI lifecycle tests exercise the hardened fetch path.
- `convex/items.ts` is edited by 003 (image lifecycle), 004 (`createLinkItem` /
  `createNoteItem` operation IDs), 007 (`createLinkItem` URL normalization), and
  008 (`finalizeItem` / `failItem` guards). Run these in table order; if any are
  branched in parallel, expect and carefully resolve `items.ts` merge conflicts.
- 002, 003, and 007 are otherwise independent after 001 and may be assigned to
  separate branches if their eventual integration follows this order, except
  that 003 and 007 both touch `convex/items.ts`, so true independence is limited
  there.

## Findings deliberately deferred

- Android uses `ActionSheetIOS` in some secondary UI paths. This is real
  cross-platform debt, but it is lower risk than the data-loss and backend
  trust-boundary work above.
- List queries and client projections will need pagination for large libraries.
  The current hard cap is a scalability constraint, not an immediate workflow
  correctness bug.
- Camera permanent-denial messaging and environment/docs cleanup are useful
  polish items, but they do not block the eight plans above.
